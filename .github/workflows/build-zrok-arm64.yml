name: Build Windows ARM64

on:
  schedule:
    - cron: '0 6 * * 1'
  workflow_dispatch:
    inputs:
      tag:
        description: 'zrok tag to build (e.g. v2.0.4)'
        required: false
        type: string

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo hello
